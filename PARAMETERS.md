# <p align='center'>Phantom | Parameters</p>

## 🧩 • Options
``` bash
# Password
password -r RANDOM

# Username
username -r RANDOM
username -r -m RANDOM, MALE
username -r -f RANDOM, FEMALE

# Email
email -r RANDOM
email -r -n RANDOM, NAMES

# Phone
phone -r RANDOM
phone -r -iso RANDOM, ISO CODE

# IPV4
--ipv4 -r RANDOM

# IPV6
--ipv6 -r RANDOM

# Get Host
--gethost -address INPUT ADDRESS
--gethost -ip INPUT IP

# Get Website
--getwebsite -status WEBSITE STATUS CODE
--getwebsite -code WEBSITE HTML CODE

# Python
--python -v VERSION
--python -p PACKAGES
--python -i INSTALL PACKAGE
--python -r REMOVE PACKAGE

```
